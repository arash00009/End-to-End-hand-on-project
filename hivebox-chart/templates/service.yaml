apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-hivebox-service
spec:
  selector:
    app: {{ .Release.Name }}-hivebox
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
